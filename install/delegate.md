---
description: Dispatch a task to a non-Claude provider with typed chains and fallback
argument-hint: "<task> [--type code-gen|bulk|research] [--read csv] [--write csv] [--accept ...] [--provider name]"
allowed-tools:
  - Bash
---

# /delegate

Run: `delegate "$ARGUMENTS"`

See `~/.claude/skills/delegate/SKILL.md` for full usage.
